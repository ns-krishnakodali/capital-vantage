spring.application.name=backend
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/cv_db}
spring.datasource.username=${DB_USERNAME:cv_user}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
